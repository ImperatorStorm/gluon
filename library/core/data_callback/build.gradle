plugins {
	id("gluon.module")
}

gluonModule {
	name = "Quilt Data Callback API"
	moduleName = "data_callback"
	hasMixins = false
	id = "quilt_data_callback"
	description = "APIs for making events or APIs data-driven"
	moduleDependencies {
		core {
			api("gluon_base")
			api("resource_loader")
			testmodOnly("networking")
		}
	}
}
