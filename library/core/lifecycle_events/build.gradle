plugins {
	id("gluon.module")
}

gluonModule {
	name = "Gluon Lifecycle Events"
	moduleName = "lifecycle_events"
	description = "Events to track the lifecycle of Minecraft."
	id = "gluon_lifecycle_events"
	moduleDependencies {
		core {
			api("gluon_base")
		}
	}
}
